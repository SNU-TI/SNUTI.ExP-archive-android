<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 배경색 (흰색) -->
    <solid android:color="#FFFFFF" />

    <!-- 테두리 (회색 선) -->
    <stroke
        android:width="1dp"
        android:color="#E0E0E0" />

    <!-- 모서리 둥글게 (이미지처럼 둥근 버튼을 위해) -->
    <corners android:radius="28dp" />
</shape>